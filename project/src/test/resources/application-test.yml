spring:
  r2dbc:
    pool:
      initial-size: 5
      max-size: 20
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      separator: ";"
  main:
    web-application-type: reactive
